# 桌面情绪释放桌宠 PRD

**版本：V1.0**  
**产品形态：桌面端桌宠应用**  
**核心功能：颂钵模式、紧箍咒模式**  
**核心目标：通过简单的鼠标点击，提供即时、低认知负担的情绪释放反馈。**

---

## 1. 产品定义

### 1.1 核心体验

用户打开应用后，桌面出现一个极简桌宠。

用户不需要学习任何规则：

> **看到 → 点击 → 得到反馈 → 情绪释放 → 恢复平静**

产品只提供两种模式：

| 模式 | 交互 | 情绪释放方式 |
|---|---|---|
| 颂钵模式 | 点击颂钵 | 敲击 + 金色声纹向外扩散 |
| 紧箍咒模式 | 点击小人 | 紧箍收紧 + 小人痛苦反馈 + 道歉 |

---

## 2. 产品范围

### 2.1 V1 必须实现

#### 基础能力

- 桌面置顶显示
- 无边框透明窗口
- 鼠标点击检测
- 模式切换
- 动画播放
- 音效播放
- 状态机控制
- 应用退出

#### 颂钵模式

- 平静状态
- 点击敲击
- 声纹生成
- 声纹扩散
- 声纹消散
- 连续点击声纹叠加

#### 紧箍咒模式

- 平静状态
- 第 1 级收紧
- 第 2 级收紧
- 第 3 级收紧
- 最大收紧
- 小人反馈
- “对不起，我错了”语音
- 停止点击后自动恢复
- 连续点击状态升级


---

# 3. 基础桌面窗口

## 3.1 窗口要求

| 字段 | 要求 |
|---|---|
| 窗口类型 | 无边框透明窗口 |
| 默认尺寸 | 400 × 400 px |
| 默认位置 | 屏幕右下区域 |
| 是否置顶 | 是 |
| 是否可拖动 | 是 |
| 是否显示任务栏图标 | 是 |
| 背景 | 完全透明 |
| 缩放 | V1 固定尺寸 |

### 注意

颂钵产生的声纹可以超出桌宠本体区域，因此动画画布需要允许声纹在较大范围内渲染。

---

# 4. 模式切换

V1 提供一个极简模式切换入口：

```text
[ 颂钵 ] [ 紧箍咒 ]
```

## 4.1 切换规则

任何状态下均允许切换。

例如：

```text
紧箍咒 MAX
    ↓
点击「颂钵」
    ↓
立即进入颂钵 IDLE
```

切换模式时：

- 清理旧模式动画
- 清理旧模式状态
- 新模式从 IDLE 开始
- 不保留上一模式状态

---

# 5. 颂钵模式

## 5.1 状态定义

```text
BOWL_IDLE
BOWL_HIT
BOWL_WAVE
BOWL_FADE
```

状态关系：

```text
BOWL_IDLE
    │
    │ 鼠标点击
    ↓
BOWL_HIT
    ↓
BOWL_WAVE
    ↓
BOWL_FADE
    ↓
BOWL_IDLE
```

---

## 5.2 BOWL_IDLE：平静状态

### 视觉

屏幕中央显示一个极简颂钵。

要求：

- 不显示文字
- 不显示按钮
- 不播放持续声音
- 轻微呼吸动画
- 微弱金色光晕
- 不主动移动位置

### 呼吸动画

周期：

`2.5～4 秒`

变化：

```text
缩放：100% → 102% → 100%
光晕：弱 → 稍强 → 弱
```

---

## 5.3 BOWL_HIT：敲击瞬间

### 触发

用户鼠标左键点击颂钵。

事件：

```text
onMouseDown
```

立即进入：

`BOWL_HIT`

### 持续时间

`100～200ms`

### 同时触发

#### 1. 钵动画

```text
scale:
100%
 ↓
97%
 ↓
100%
```

表现为被敲击后的轻微震动。

#### 2. 音效

播放：

`bowl_hit.wav`

要求：

- 约 200～1000ms
- 有明显颂钵共鸣
- 不尖锐
- 具有余韵

#### 3. 声纹生成

创建新的 `WaveObject`。

```json
{
  "id": "uuid",
  "createdAt": 0,
  "radius": 20,
  "opacity": 1,
  "speed": 300,
  "lifeTime": 2000
}
```

---

## 5.4 BOWL_WAVE：声纹扩散

声纹从颂钵向外扩散。

### 动画

```text
radius:
20px
 ↓
100px
 ↓
300px
 ↓
600px
 ↓
屏幕边缘
```

### 视觉要求

不是简单的圆形边框。

建议：

- 金色线条
- 多层半透明线
- 不规则波动
- 少量粒子
- 中心较亮
- 外圈逐渐透明

整体感觉：

> 声音变成一圈金色能量，从颂钵向外传播。

---

## 5.5 BOWL_FADE：声纹消散

当声纹接近屏幕边界：

```text
opacity:
1
 ↓
0.7
 ↓
0.3
 ↓
0
```

完成后：

```text
WaveObject.destroy()
```

生命周期：

```text
CREATE
 ↓
EXPAND
 ↓
FADE
 ↓
DESTROY
```

---

## 5.6 颂钵连续点击

这是颂钵模式的核心交互之一。

用户连续点击：

```text
Click 1
 ↓
Wave 1

Click 2
 ↓
Wave 1 + Wave 2

Click 3
 ↓
Wave 1 + Wave 2 + Wave 3
```

每次点击都必须创建独立的 `WaveObject`。

**不能重置上一圈声纹。**

这样用户连续敲击时，可以看到多个金色声纹同时向外传播。

---
5.7 高频点击彩蛋：法海模式

当用户在颂钵模式下连续、高频点击时，触发一次隐藏彩蛋。

触发条件

在任意连续 30 秒时间窗口内：

点击次数 > 30 次

即触发提示。

建议实现为滑动时间窗口：每次点击记录时间戳，只保留最近 30 秒内的点击记录，并实时判断数量。

触发效果

弹出一句话：

你是法海，在这收妖呢？

展现方式

文字出现在桌宠附近

不阻塞鼠标操作

建议采用轻量气泡/浮字动画

出现后停留约 2～3 秒

然后淡出

不改变颂钵当前状态

已经产生的声纹继续正常播放和消散

触发后规则

同一轮高频点击达到阈值后，不应每次点击重复弹出。

30 秒内 > 30 次
        ↓
触发一次彩蛋
        ↓
进入冷却状态
        ↓
冷却结束后重新计算

建议冷却时间：30 秒。

冷却期间仍然记录点击，但不再次弹出提示。

# 6. 紧箍咒模式

## 6.1 状态定义

```text
CIRCLE_IDLE

CIRCLE_PRESS_1
CIRCLE_PRESS_2
CIRCLE_PRESS_3
CIRCLE_MAX

CIRCLE_RELEASE
```

状态关系：

```text
                 点击
CIRCLE_IDLE ─────────────→ PRESS_1
                              │
                            点击
                              ↓
                           PRESS_2
                              │
                            点击
                              ↓
                           PRESS_3
                              │
                            点击
                              ↓
                             MAX
                              │
                         停止点击
                              ↓
                           RELEASE
                              ↓
                             IDLE
```

---

# 6.2. 小人视觉

V1 使用非常简单的抽象粗线条风格。

组成：

- 圆形头部
- 粗线条身体
- 简单四肢
- 紧箍
- 极简表情

示意：

```text
      ─────
       ○
      /|\
      / \
```

设计原则：

> **故意简单、粗糙、略带荒诞感。**

不需要复杂人物建模。

---

# 6.3. CIRCLE_IDLE：平静状态

默认状态：

```text
紧箍：松弛
人物：正常站立
表情：平静
```

动画：

- 轻微呼吸
- 身体上下 1～2px
- 紧箍无主动变化

---

# 6.4. 第一次点击

状态：

```text
CIRCLE_IDLE
    ↓ 点击
CIRCLE_PRESS_1
```

### 紧箍

收紧约：

`20%`

### 小人

- 身体轻微缩小
- 头部向下
- 表情变痛苦
- 身体抖动一次

### 语音

播放：

> “对不起，我错了。”

### 动画时长

`600～1000ms`

---

# 6.5. 第二次点击

状态：

`CIRCLE_PRESS_2`

### 紧箍

收紧至：

`40%`

### 小人

- 身体弯曲
- 手靠近头部
- 头部向下
- 抖动更明显

### 语音

> “对不起，我错了。”

声音语气比第一次更急促。

---

# 6.6. 第三次点击

状态：

`CIRCLE_PRESS_3`

### 紧箍

收紧至：

`70%`

### 小人

- 明显痛苦
- 双手抱头
- 身体弯曲
- 高频抖动

### 语音

> “对不起！我错了！”

---

# 6.7. CIRCLE_MAX：最大状态

状态：

`CIRCLE_MAX`

### 紧箍

收紧至：

`90～100%`

### 小人

- 抱头
- 剧烈抖动
- 极端痛苦表情
- 身体发生夸张变形

### 语音

> **“对不起！我错了！”**

### MAX 后继续点击

状态保持：

```text
MAX
 ↓ 点击
MAX
```

但每次点击：

- 再触发一次抖动
- 再播放一次语音

**不继续增加压力等级。**

---

# 6.8. 停止点击与恢复

## 6.8.1 停止点击判断

维护：

```typescript
lastClickTime
```

如果：

```text
currentTime - lastClickTime >= 1500ms
```

则进入：

`CIRCLE_RELEASE`

即用户停止点击 **1.5 秒**后开始恢复。

---

# 6.9. CIRCLE_RELEASE：恢复状态

恢复过程不是瞬间跳转，而是连续动画。

```text
MAX
 ↓
PRESS_3
 ↓
PRESS_2
 ↓
PRESS_1
 ↓
IDLE
```

### 紧箍恢复

```text
100%
 ↓ 400ms
70%
 ↓ 400ms
40%
 ↓ 400ms
20%
 ↓ 400ms
0%
```

### 人物同步恢复

```text
剧烈痛苦
 ↓
痛苦
 ↓
难受
 ↓
平静
```

### 总恢复时间

约：

`1.5～2 秒`

恢复过程中：

- 不播放语音
- 不产生新的反馈

---

# 7. 鼠标点击规则

必须区分鼠标点击和长按。

## 7.1 点击

```text
mousedown
    ↓
触发一次
```

## 7.2 长按

```text
mousedown
    ↓
触发一次

持续按住
    ↓
不重复触发

mouseup
    ↓
无动作
```

因此一次鼠标按下只产生一次释放动作。

---

# 8. 声音系统

V1 只需要两类声音。

## 8.1 颂钵

建议至少准备：

```text
bowl_hit_01.wav
bowl_hit_02.wav
bowl_hit_03.wav
```

每次点击随机选择一个。

目的：

避免连续点击时声音完全机械重复。

---

## 8.2 紧箍咒

建议：

```text
sorry_01.wav
sorry_02.wav
sorry_03.wav
```

按照压力等级选择。

---

# 9. 声音播放规则

声音不能无限叠加。

### 颂钵

允许多个声纹同时存在。

声音按照点击触发：

```text
点击
 ↓
播放敲击声
```

如果连续点击：

- 每次点击都播放
- 最大同时播放声音实例数：4
- 超过 4 个时，停止/回收最早的声音实例

### 紧箍咒

每次点击播放对应等级语音。

MAX 状态下继续点击仍可播放语音，但不能无限创建音频实例。

---

# 10. 核心数据结构

## 10.1 Mode

```typescript
enum Mode {
  BOWL,
  CIRCLE
}
```

## 10.2 BowlState

```typescript
enum BowlState {
  IDLE,
  HIT,
  WAVE,
  FADE
}
```

## 10.3 CircleState

```typescript
enum CircleState {
  IDLE,
  PRESS_1,
  PRESS_2,
  PRESS_3,
  MAX,
  RELEASE
}
```

---

# 11. 声纹数据结构

```typescript
interface WaveObject {
  id: string;
  createdAt: number;
  radius: number;
  opacity: number;
  speed: number;
  lifeTime: number;
}
```

建议默认：

```text
radius = 20
speed = 300～500 px/s
opacity = 1
lifeTime = 根据屏幕尺寸动态计算
```

---

# 12. 紧箍状态数据

```typescript
interface CirclePressure {
  level: number;
  ringTightness: number;
  bodyDeformation: number;
  shakeIntensity: number;
  voiceLevel: number;
}
```

建议：

| Level | Tightness | Deformation | Shake |
|---:|---:|---:|---:|
| 0 | 0 | 0 | 0 |
| 1 | 0.2 | 0.1 | 0.1 |
| 2 | 0.4 | 0.3 | 0.3 |
| 3 | 0.7 | 0.6 | 0.6 |
| 4 | 1.0 | 1.0 | 1.0 |

---

# 13. 全局状态

```typescript
interface AppState {
  mode: Mode;

  bowlState: BowlState;

  circleState: CircleState;
  circleLevel: number;
  lastCircleClickTime: number;

  waves: WaveObject[];

  soundEnabled: boolean;
}
```

---

# 14. 模式切换逻辑

```typescript
switchMode(BOWL)

→ mode = BOWL
→ bowlState = IDLE
→ waves = []
→ circleState = IDLE
→ circleLevel = 0
```

```typescript
switchMode(CIRCLE)

→ mode = CIRCLE
→ bowlState = IDLE
→ waves = []
→ circleState = IDLE
→ circleLevel = 0
```

**切换模式必须清理原模式动画。**

---

# 15. 异常情况

| 场景 | 处理 |
|---|---|
| 快速连续点击颂钵 | 正常响应，每次生成新声纹 |
| 快速连续点击紧箍 | 正常升级 |
| 紧箍达到 MAX | 不再升级 |
| MAX 后继续点击 | 保持 MAX，重复反馈 |
| 长按鼠标 | 只触发一次 |
| 切换模式时存在动画 | 立即清理旧动画 |
| 窗口失去焦点 | 不改变状态 |
| 应用重新启动 | 从 IDLE 开始 |
| 声音实例过多 | 按上限回收旧实例 |

---

# 16. 验收标准

## 16.1 颂钵模式

- [ ] 启动后看到颂钵
- [ ] 颂钵处于平静状态
- [ ] 点击后产生明确敲击反馈
- [ ] 点击后播放颂钵声音
- [ ] 点击后产生金色声纹
- [ ] 声纹从颂钵向外扩散
- [ ] 声纹到达屏幕边缘后消散
- [ ] 连续点击可以产生多个声纹
- [ ] 多个声纹可以同时存在
- [ ] 声音不会无限叠加
- [ ] 无操作时保持平静

## 16.2 紧箍咒模式

- [ ] 启动后看到极简小人
- [ ] 小人头上有紧箍
- [ ] 第一次点击紧箍收紧
- [ ] 第二次点击进一步收紧
- [ ] 第三次点击进一步收紧
- [ ] 第四次点击进入 MAX
- [ ] MAX 后不继续增加等级
- [ ] 每次点击都有小人反馈
- [ ] 小人说“对不起，我错了”
- [ ] 停止点击 1.5 秒后开始恢复
- [ ] 紧箍逐渐松开
- [ ] 小人逐渐恢复平静
- [ ] 恢复后回到 IDLE

## 17.3 模式切换

- [ ] 任意状态下可以切换模式
- [ ] 切换后旧模式动画立即清除
- [ ] 新模式从 IDLE 开始
- [ ] 不继承旧模式压力/动画状态

---

#
# 18. 产品最终状态机

## 颂钵

```text
       点击
        ↓
     ┌──────┐
     │ 敲击 │
     └──┬───┘
        ↓
   金色声纹扩散
        ↓
      消散
        ↓
      平静
```

## 紧箍咒

```text
       点击
        ↓
      收紧
        ↓
    “对不起”
        ↓
       点击
        ↓
     更加收紧
        ↓
       ...
        ↓
       MAX
        ↓
     停止点击
        ↓
      慢慢松开
        ↓
       平静
```

---


